# 生产环境
VITE_APP_ENV = 'production'
# 应用访问路径前缀：单栈（Java+Vue）部署，根路径直出（nginx 根 location 服务 portal SPA）
VITE_APP_BASE_PATH = '/'
# 后端 API 基础路径：同域根路径直连（边缘 nginx /api/ → java-nginx → java-backend /api/）
VITE_API_BASE = '/api/v1'
