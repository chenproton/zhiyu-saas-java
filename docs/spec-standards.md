# Spec 模式规范（规格先行开发约定）

> 本仓库以「规格先行（spec-first）」模式由 AI 主导开发：**先写规格、再写代码、代码必须回写规格**。本文档约束 spec 的层级、模板、验收标准，以及代码与 spec 的一致性关系。
>
> 适用范围：`docs/spec/` 下所有规格文档，以及所有「新增/变更功能」的开发流程。

## 一、Spec 的分级与定位

Spec 分两级，按「覆盖范围」与「该文档在何时被阅读」区分：

| 层级 | 文件 | 定位 | 何时写 / 何时读 |
|---|---|---|---|
| 全平台规格 | `01-prd.md` ~ `05-prototype-interaction.md` | 一期已实现能力的**回溯沉淀**，是「当前系统能力基线」 | 做涉及现有功能的改动前必读；改动后回写 |
| 子平台/新功能规格 | `partner-enterprise-platform.md` 等 | 单个大模块/新平台的**完整前瞻规格**（PRD + API + 数据 + 计划合一） | 立项新模块时新建；实施期间持续更新 |

判断用哪级：**改现有能力 → 回写全平台规格；新增独立大模块 → 新建子平台规格**。小的 bug 修复、纯重构（行为不变）不需要动 spec，但需在 commit message 说明「行为不变，不影响 spec」。

## 二、全平台规格的五层（必须齐备，按需增补）

| 序号 | 文件 | 必须回答的问题 |
|---|---|---|
| 01 | 产品需求文档（PRD） | 为什么做、给谁用、解决什么痛点、功能边界、成功指标 |
| 02 | 接口契约 | 每个模块有哪些端点、鉴权、请求/响应形状、错误码、通用约定 |
| 03 | 开发计划（WBS） | 里程碑、任务拆解、依赖、风险、评审节点 |
| 04 | 数据库 Schema | ER 关系、字段级表定义、租户隔离方式、数据字典 |
| 05 | 原型/交互 | 页面清单、关键交互、全局规则、响应式 |

**每一层都必须与代码一致**：spec 描述的能力，代码必须实现；代码实现的行为，spec 必须记录。二者任一变化，另一者需同步更新（见「六、一致性红线」）。

## 三、子平台规格的模板（新建大模块时套用）

新建 `docs/spec/<模块名>.md`，按以下章节顺序写（非必需章节可标「N/A」）：

```
# <模块名>规格文档 — 知与 SaaS

## 1. 背景与目标
## 2. 平台级架构决策（技术选型、数据边界、与其他模块的关系）
## 3. 核心流程（用户故事 + 时序）
## 4. 数据模型（表/字段/租户隔离）
## 5. API 契约（端点清单 + 代表性接口详写）
## 6. 后端开发计划（WBS）
## 7. 前端开发计划（WBS）
## 8. 部署与验证（质量门禁、验收标准）
## 9. 实施顺序与风险
## 10. 扩展性预留（明确「暂不做」的东西，防止过度设计）
```

章节 2、10 是防「过度设计」和「误改」的关键：写清「为什么这么选」和「明确不做什么」，让后续 AI 不重复造轮子、不做 speculative 功能。

## 四、验收标准（DoD，Definition of Done）

任何「功能开发任务」只有在同时满足以下条件才算完成：

1. **代码** 实现 spec 描述的能力，通过 `deploy.sh` 质量门禁（后端 gofmt/vet/build + 前端 typecheck/lint/test）。
2. **规格** 已同步：新增能力写入对应 spec；变更行为更新 spec 对应章节。
3. **测试** 至少一种（handler/service/store 单测，见 `refactor-layering.md` 红线）。
4. **契约** API 变更同步 `02-api-contract.md` 或子平台 spec 的 API 章节；migration 配对 `.down.sql`。
5. **边界** spec 的「扩展性预留」未写「暂不做」的东西，若实现发现实际做了，要么补 spec，要么删除代码。

## 五、规格的放置与命名

- 全平台 spec 固定在 `docs/spec/`，编号 `01`~`05`。
- 子平台 spec 放在 `docs/spec/`，命名 `<模块英文名>.md`（小写连字符），不编号，因为一个模块是独立范围。
- **禁止**把 spec 散落到 `docs/` 根目录或各 `backend/`/`apps/` 内部；spec 是「全项目的单一事实源」，集中一处，任何 AI 协作者都先读这里。
- 规格文档不设「教程 vs 参考」之分（见 `documentation-standards.md`）：spec 一律是**参考型**，按需查，不要求顺序通读。

## 六、一致性红线（代码 ↔ spec 不得偏航）

1. **新增能力**：必须先（或同时）在 spec 记录「要做什么」，再写代码；不允许「只写代码不改 spec」。
2. **行为变更**：改了接口返回形状、状态机、错误码、权限，必须同步 spec；纯重构（行为不变）可不动。
3. **删除能力**：从代码删功能时，同步从 spec 删除对应描述，并检查是否有其他 spec 章节引用它。
4. **AI 协作者的首要检查**：接到需求，先 `read` 相关 spec，确认现状与「扩展性预留」；若 spec 未覆盖，先补 spec 再动手（这是 spec-first 的硬约束）。

## 七、AI 协作者约定（spec-first 红线）

- 每个功能任务的第一步是**读 spec**，不是读代码。spec 是意图，代码是实现，先对齐意图。
- 发现「spec 说的」与「代码做的」不一致时，**停下来向用户报告差异**，不要擅自判定谁对谁错（可能是 spec 过时，也可能是代码偏离了本意）。
- 提交信息里，功能类改动需引用对应 spec 文件名/章节（如「见 docs/spec/02-api-contract.md §2」），便于追溯。
