# 全量代码审查问题清单（2026-08-09，未修复项）

> 配套勾选清单：`checklist.md`（876 文件全部检查完成）。
> 已修复问题已删除。修复提交：53c12dd7 越权批、19a7cbcc 前端批、d46a993e 中危后端批、16ef8d3e 中危前端批、c876e8d8 低危清理批、44e7252d 冻结概念清除。

## 未修复统计

| 级别 | 数量 | 说明 |
|------|------|------|
| 高危 | **0** | 无未鉴权接口、无 SQL 注入、无密钥泄露、无 XSS |
| 中危 | **1 条** | 发布钩子长事务（评估保持，见跳过表） |
| 低危 | **约 65 条** | any/风格/常量体系类，按"简单优先"容忍（limit 魔数/死 setter/toLocaleString 已清） |
| 评估跳过 | 7 条 | 见跳过表 |

## 剩余中危（0 条）

全部已处理：发布钩子评估保持（原子性价值 > 超时风险，量级可控）；测试盲区已补权限矩阵回归测试（role_isolation_test.go），事务回滚/导入测试按迭代补。

## 评估跳过项（成本/风险 > 收益，5 条）

| # | 位置 | 原因 |
|---|------|------|
| 1 | dict_store 基类租户过滤 | 影响 22 个 handler 调用，handler 已全部兜底校验 |
| 2 | bank-question-selector-panel 逐题 N+1 | 已有缓存+并行兜底 |
| 3 | 发布钩子长事务（lesson_content.go:390-418） | 分批提交破坏"发布=原子"语义；100 节点约 1-5s 在 30s 超时内可控 |
| 4 | 测试事务回滚/导入覆盖 | 构造失败导入需大量测试夹具，随迭代补 |
| 5 | 富文本编辑器 | 已删除装饰工具栏并明示纯文本；如需富文本需引入 TipTap 库 |

## 低危（约 90 条，已评估可容忍）

- any 滥用 ~230 处（approvals/审批已类型化；其余集中在 tasks/scene-results/alliance 品牌页）（history:any/mapRecord(a:any)/props:any 等，均可用已有类型替换）
- 硬编码状态字符串 ~74 处（draft/published 数组，需统一常量体系）
- 性能 ~5 处（N+1/相关子查询，量级可控；limit 魔数已清）
- i18n 缺失 ~5 处（ui 包中文为主，需 ui 包字典）
- 风格 ~10 处（嵌套三元/命名/超大文件——按约定不拆）
- 存量 SQL 字面量 ~190 条（已无 Pool，纯组织优化，不影响功能性能）
- 其余 ~25 条（死代码残留/细节，随迭代清理）

## 备注

- 中危已全部处理（修复或评估保持），无现役数据风险
- import/export/template 23 文件已迁移：Pool 全部移除、Store 注入统一；全仓库无冻结/豁免概念，所有内容均可调整
