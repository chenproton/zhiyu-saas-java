# 双栈对齐剩余迁移计划（feat/agent-align-remaining）

> 分支：`feat/agent-align-remaining`（基于 origin/master = e3fffade，工作区干净）。
> 目标：逐个完成 Java（复制版）对齐 Go（原始版）的剩余迁移项，做到「全部完成迁移」。
> 方法：每完成一项即单独 commit，保持「单次 commit 只含当次变更」。

## 已完成（上一批，已合并 master e3fffade）

- 6 类 Excel 导入落库、3 类导出补全、任务权重端点+前端契约、报名 API、共建审批合并回源、AI 核心 chat + 结构化生成 + AES-GCM、前端 publish+URL 别名、组织 rename、课程测评生成补全、资源 find-or-create、Question key 核对。

## 剩余项清单（按执行顺序，含依赖与 Go 依据）

### A. 品牌 Excel 导入深链（P1 · 后端）
- Go 依据：`service/resource_import.go DoImportBrandsTyped` + `store/alliance_brand_import.go`
- 现状：Java `ImportExportServiceImpl.importBrands` 仅 name/type 落库，学生/企业/岗位/专业/教师/专家 ID 全不解析，无查重/overwrite/rename。
- 要做：
  1. job → 建岗位 + SaveFull，回填 position_id；
  2. teacher → 按 user_id 查/建专家，回填 expert_id/teacher_id；
  3. employer → 解析并关联 enterprise_id；
  4. talent/student → 解析 student_id；major → 解析 major_id；
  5. GetBrandByName 查重 + overwrite/rename/skip。
- 依赖：无（可先做）。

### B. AiKbChunk 文档分块 + AI 中心 RAG 对话（P0-5c · 后端，最大）
- Go 依据：`store/ai_center.go InsertChunks/SearchChunks/FinishDocumentParse` + `service/ai_center_agent.go/ai_center_kb.go/ai_center_retrieval.go`
- 现状：Java 上传文档直接置 ready、chunkCount=0，无 AiKbChunk 实体；agentChat/kbAsk/yiknowChat 返回 mock。
- 要做：
  1. 新增 `AiKbChunk` 实体 + `AiKbChunkMapper`（chunks 表：分块文本 + 向量/关键词检索）；
  2. 文档上传后解析分块并入库（InsertChunks / FinishDocumentParse）；
  3. kbAsk/agentChat/yiknowChat 走 RAG 检索（SearchChunks）+ 真实 ChatCompletion。
- 依赖：需先读 Go 的检索/分块 SQL 与 chunks 表结构；体量最大，排在中后。

### C. 快照版本 bump 补回（P2 · 后端）
- Go 依据：`MergePositionDraftToSource/MergeScenarioDraftToSource` 末尾 `BumpVersionAndSnapshot`
- 现状：合并回源后未 bump 版本 + 未写快照。
- 要做：合并事务内补 source 资源版本 bump + saveSnapshot（岗位/场景快照 JSON）。

### D. affairs/lesson 实体归位（P1 · 后端重构）
- Go 依据：`store/terms.go` + `store/scheduling.go` + `store/courses.go`
- 现状：Term/Venue/PeriodSlot/ScheduleEntry 复用 domain/portal 的 Portal*；课程实体 PortalCourse 落 domain/portal。
- 要做：迁为 domain/affairs 与 domain/lesson 下对应实体 + mapper，controller/service/entity/mapper 同域。
- 依赖：纯重构，行为不变，最后做（避免影响前面功能改动）。

### E. 前端 /superadmin 控制台（P1 · 前端）
- Go/React 依据：`frontend/edu/app/superadmin` + `adminAiApi`
- 现状：Vue 无 superadmin 页。
- 要做：整页（租户/套餐/租户级 AI 配置）+ 路由 + adminAiApi。

### F. 需决策/谨慎项（P2，先记录，按用户决策处理）
1. **多出端点收敛**：✅ 已删除对齐（5 个端点，用户拍板删除）。
2. **鉴权注解**：⏸ 已定位为「鉴权模型差异」，非功能对齐项——Java 用 ZhiyuAuthFilter 全局 JWT + TenantContext 租户隔离 + 业务层归属校验（requireTenant/checkXxx），Go 用菜单驱动 RBAC（RequireMenu/RequireRole）。补 @SaCheckPermission 需先建 Sa-Token 权限体系 + 迁移 menu_grants.go 授权映射，属独立安全专项，建议单独立项。

## 执行顺序（每步单独 commit）

1. A 品牌导入深链 —— ✅ 已完成（子代理 brand-import，编译 exit 0，报告 gap-brand-import-deeplink.md）
2. B AiKbChunk + AI 中心 RAG —— ✅ 已完成（子代理 ai-center-rag，编译 exit 0，报告 gap-ai-center-rag.md；检索可见性过滤 published/owner/协作者 已核对）
3. C 快照 bump —— ✅ 已完成（version+0.1 部分；快照重建仍暂缓 P2）
4. D 实体归位 —— ✅ 已完成（子代理 entity-relocate，编译 exit 0，报告 gap-affairs-lesson-entity.md）
5. E /superadmin 前端 —— ✅ 已完成（子代理，typecheck exit 0，报告 gap-superadmin.md）
6. F 决策项 —— 多出端点删除 ✅；鉴权注解 ⏸ 属独立安全专项（见 F 节）

> 每完成一项：编译/typecheck 校验 + 回写本计划状态 + 单独 commit。
