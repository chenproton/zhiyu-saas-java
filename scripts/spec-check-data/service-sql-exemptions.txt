# service-sql-exemptions.txt — spec-check.sh 第 1 项「service 不得拼接 SQL」的豁免名单
#
# 格式：每行一个 service 文件路径，相对 service 目录根
#       （ruoyi-modules/ruoyi-zhiyu/src/main/java/org/dromara/zhiyu/service 下，
#        即 impl/<域>/<类名>.java 或 <域>/<类名>.java）；以 # 开头为注释。
#       默认严格（出现裸 SELECT/INSERT/UPDATE/DELETE 关键字即违规），
#       仅「合法常量 SQL 片段」可豁免——即非用户输入拼接的固定查询片段。
#
# 豁免项：
#  - impl/portal/WorkspaceServiceImpl.java：MyBatis-Plus QueryWrapper.apply("EXISTS (SELECT 1 FROM ...")
#    构造 EXISTS 子查询条件，{0} 参数化占位、无用户输入字符串拼接，属合法常量 SQL 片段。
#  - impl/portal/CommunityServiceImpl.java：QueryWrapper.last("ORDER BY COALESCE((SELECT ...")
#    固定排序 SQL 片段（常量），无用户输入拼接。
impl/portal/WorkspaceServiceImpl.java
impl/portal/CommunityServiceImpl.java
